[theme]
# Demografy official brand colours (from Brand Guidelines PDF)
primaryColor = "#9a66ee"              # Medium Slate Blue — main buttons, highlights
backgroundColor = "#ffffff"           # White background
secondaryBackgroundColor = "#f3eeff"  # Very light purple tint
textColor = "#272d2d"                 # Jet Black — body text
font = "sans serif"

[server]
port = 8502
